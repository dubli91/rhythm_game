# Implementation Plan

## Status

Milestones 0–3 are COMPLETE as of 2026-07-15. Milestone 0 (scaffolding) and Milestone 1 (`src/lib` stdlib) — Vite+TS+PixiJS+Biome+Vitest scaffold in place, all 10 `src/lib` modules implemented with 100 passing unit tests, `npm run build`/`typecheck`/`lint`/`test` all green. Milestone 2 delivered the full playable vertical slice: app shell + DOM screens (`src/app/shell.ts`, `main.ts`), screen state machine (`src/app/screens.ts`, 35 tests), judgement engine + scoring (32 tests), gauge engine all 5 types + clear lamps (24 tests), play input (17 tests), song player with 1s lead-in + 300ms fade-out stop (13 tests), PixiJS playfield renderer + HUD (`src/features/play/render.ts`), play-session controller (`src/features/play/controller.ts`), built-in song "First Light" (`song-6f90aea6`, NORMAL ☆4 = 145 notes / HYPER ☆7 = 332 notes, generated by `scripts/generate-builtin-song.mjs`, 5 validation tests). 226 unit tests green; typecheck/lint/build green; e2e-verified via `scripts/verify-e2e.mjs` (title→select→play→abandon→results→autoplay→retry, zero page errors). Milestone 3 delivered results & local records: records store (`src/features/records/store.ts`, localStorage `records.v1` via createLocalDoc, 14 unit tests), deterministic ID lib (`src/lib/chart/ids.ts`, 7 tests), PlayResult extended with songId/chartId/hiSpeed/arrangement, results screen shows title/artist/difficulty, options line (HS/arrangement), CLEAR LAMP row, NEW RECORD badges and ±EX diff vs best / FIRST PLAY. 247 unit tests green; typecheck/lint/build green; e2e-verified via `scripts/verify-e2e.mjs` AND new `scripts/verify-records-e2e.mjs` (records.v1 contents, autoplay-no-write, reload persistence, corrupt-backup-then-prompt). Milestone 4 (song library & song select) is PARTIALLY complete as of 2026-07-15: unified song library (built-in + IndexedDB merge, `src/features/songs/library.ts`, 10 tests), song-select screen rewrite (grouped song rows → expandable difficulty rows, headless select model `src/features/select/model.ts` with 17 tests, per-chart lamp/rank/EX record display, 3 sort modes persisted at `select.v1`, keyboard+mouse), hi-speed adjust from the options bar (←/→, persisted), and 3 built-in songs (spec builtin-song-content MUST 1-4 satisfied). 286 unit tests green; typecheck/lint/build green; both e2e scripts pass (verify-e2e.mjs now also play-smokes Neon Cascade NORMAL and Overdrive Core ANOTHER via autoplay). Remaining M4 item: navigation entry points from select to SETTINGS/PRACTICE_EDIT (song-select.md MUST 10 violation today — the screen machine already permits those transitions but shell.ts never fires them and no DOM exists for those screens; blocked on those screens actually existing, so it lands with Milestone 6/7 rather than as a stub). Milestone 5 (full play options) is COMPLETE as of 2026-07-16: hi-speed/SUDDEN+/arrangement/autoplay per play-options.md MUST 1-12, 306 unit tests green, typecheck/lint/build green, both e2e scripts extended and passing (options-panel persistence, in-play adjustment keys, RANDOM-autoplay judgement invariance, MIRROR dense-chart smoke). Milestone 6 (practice mode) is COMPLETE as of 2026-07-16 per practice-mode.md MUST 1-11 + SHOULD 12/14 (grid editor, presets, IndexedDB save/load/delete, seamless loop playback with metronome count-in, real judge/renderer/input reuse, per-loop + cumulative stats, next-loop BPM change, target-loop auto-end): 387 unit tests green, typecheck/lint/build green, both e2e scripts pass (verify-e2e.mjs gained a full practice section: editor entry via P, keyboard grid editing, trill preset, save→IndexedDB→reload persistence, 2-loop auto-end summary, records.v1-untouched assertion). Also note the song-select→PRACTICE_EDIT navigation entry (M4 leftover) landed with it; only the SETTINGS entry point remains (blocked on Milestone 7's settings screen). Milestone 7 is PARTIALLY complete as of 2026-07-17: the unified settings screen shipped per settings-screen.md MUST 1-11 + SHOULD 12 (key config with capture/conflict/reserved-key rejection, per-lane + all-lane reset, global offset −200..+200ms stepper+slider, master/music/effects volume sliders with live GainNode apply and effects preview click, offset-calibration modal with 16-tap mean-error proposal seeded by outputLatency), entered from song select via KeyO (closing the last M4 leftover, song-select.md MUST 10 now fully satisfied). CN (charge notes) landed end-to-end on 2026-07-17 per chart-format.md SHOULD 9 / judgement-scoring.md SHOULD 12 / playfield-rendering.md SHOULD 14 + builtin-song-content.md SHOULD 15 (engine + renderer + validator + Neon Cascade showcase content), 457 unit tests green, both e2e scripts pass. 418 unit tests green; typecheck/lint/build green; verify-e2e.mjs gained a full settings section (entry, capture flow with conflict highlight + reserved rejection, offset/volume keyboard adjust + persistence, calibration cancel/apply, reload persistence, reset-all, corrupt settings.v1 fallback) and verify-records-e2e.mjs still passes. Also note: the vestigial IMPORT screen was removed from screens.ts/screens.test.ts (spec compliance after the 2026-07-16 BMS descope). Remaining M7 items are the SHOULD-tier polish (song preview, dev overlays, stats screen + records export/import, level folder/search) plus the M5 green-number SHOULD. Menu SFX (move/confirm/cancel, audio-playback.md MUST 8) landed 2026-07-17 via createMenuSfx on the effects bus — with it, every MUST across all specs is now implemented; 465 unit tests green and both e2e scripts pass. Song preview on select landed 2026-07-17 (audio-playback.md/song-select.md SHOULD 12; `src/features/select/preview.ts`, 479 unit tests green, both e2e scripts extended/passing). Remaining work is SHOULD-tier only: dev overlays, stats screen + records export/import, level folder/search, M5 green-number, M6 δ histogram. Player statistics + records export/import landed 2026-07-17 (results-records.md SHOULD 10/11 + settings-screen.md SHOULD 13; in-settings RECORDS section, specs reconciled the same day; 506 unit tests green, both e2e scripts extended/passing). Song-select search + level folder view landed 2026-07-17 (song-select.md SHOULD 11/13 — with them song-select.md is fully implemented; 521 unit tests green, both e2e scripts extended/passing). Green-number readout landed 2026-07-17 (play-options.md SHOULD 13 — with it Milestone 5 is fully complete; 526 unit tests green, both e2e scripts extended/passing). Dev overlays (playfield-rendering.md SHOULD 16 + input-handling.md SHOULD 10) and the M6 δ histogram (practice-mode.md SHOULD 13) landed 2026-07-17, closing the last SHOULD-tier items (546 unit tests green, both e2e scripts pass; see the M7 dev-overlays and M6 learnings addenda). The ogg conversion landed 2026-07-17 — built-in audio is now ogg vorbis 44.1kHz stereo (MUST 9 satisfied), encoded at generation time by the new `wasm-media-encoders` devDependency (libvorbis WASM, no system ffmpeg); 546 unit tests green, typecheck/lint/build green, both e2e scripts pass against the ogg assets (real decodeAudioData in Chromium).

**Spec compliance is NO LONGER complete (2026-07-17).** After the milestones above reached full MUST+SHOULD coverage, four specs gained UNCOMMITTED new MUST-tier requirements (see `git diff specs/`), adding THREE new features that are entirely unimplemented in `src/` (verified by code inspection, not just the audit): **(A) scratch secondary key** — input-handling.md MUST 12-14 + settings-screen.md MUST 15; **(B) green-number lock / floating hi-speed** — play-options.md MUST 15-17; **(C) judgement explosion effect** — playfield-rendering.md MUST 17. These land in the new **Milestone 8** below (MUST-tier, prioritized C→B→A by user-impact/risk). A full 15-spec + 6-lib audit alongside also surfaced a few minor pre-existing spec deviations and dead-code cleanup items, folded into Milestone 8's follow-ups + cleanup subsections. No spec-less element was found — every gap has an owning spec already (the three new MUSTs' specs are the four uncommitted diffs), so NO new `specs/*.md` needs authoring this round. Remaining work: Milestone 8 (3 new MUSTs) + minor spec-deviation follow-ups + the standing vestigial-BMS/dead-code cleanup pass. UPDATE 2026-07-18: ALL THREE landed — (C) judgement explosion + (B) green-number lock (tag 0.0.15), then (A) scratch secondary key (tag 0.0.16); 570 unit tests green, both e2e suites pass. Spec compliance is COMPLETE again — every MUST and SHOULD across all specs is implemented. Remaining work is the minor-deviation follow-ups + the standing vestigial-BMS/dead-code cleanup pass. UPDATE 2026-07-18 (later): a NEW MUST-tier feature was spec'd across four specs (uncommitted diffs in `specs/`): **(D) FAST/SLOW timing indicator** — judgement-scoring.md MUST 14-16 (δ-sign classification + counts), play-options.md MUST 18 (display mode option), playfield-rendering.md MUST 18 (on-play display), results-records.md MUST 13 (results-screen counts, records schema unchanged). Entirely unimplemented in `src/`; tracked as **Milestone 9** below. UPDATE 2026-07-18 (later still): Milestone 9 landed the SAME DAY (tag 0.0.17) — 587 unit tests green, typecheck/lint/build green, both e2e suites pass. Spec compliance is COMPLETE again; remaining work is the minor-deviation follow-ups + the standing vestigial-BMS/dead-code cleanup pass. UPDATE 2026-07-21: a NEW top-priority spec landed — `specs/practice-song-content.md` (no-BGM keysound practice song, BPM 282 chord+scratch, marked 최우선). Implemented the same day as **Milestone 10** below (tag 0.0.18): all MUSTs + SHOULD 13/14, 619 unit tests green, typecheck/lint/build green, both e2e suites pass (verify-e2e gained 3 practice-song steps). Spec compliance is COMPLETE again; 5 cross-spec wording inconsistencies the new spec introduced were reconciled by an Opus subagent the same day (see M10 learnings). UPDATE 2026-07-22: **Milestone 11** landed (tag 0.0.19) — results-screen δ-histogram sparkline with the accumulator unified into the scorer (judgement-scoring.md SHOULD 13, spec wording pinned same day), gauge-out death freeze+fade presentation (playfield-rendering.md SHOULD 15 — the last unimplemented SHOULD), and a REAL judgement-correctness bug fix in the master clock: `getOutputTimestamp()` readings are now plausibility-guarded against the sync-sampled skew (audio-playback.md MUST 6 amended). 627 unit tests green; typecheck/lint/build green; both e2e suites pass. Spec compliance is COMPLETE — every MUST and SHOULD across all specs is implemented. UPDATE 2026-07-22 (later): **Milestone 12** landed (tag 0.0.20) — a full 16-spec/6-agent audit reconfirmed compliance and found exactly ONE gap, builtin-song-content.md SHOULD 14 (standalone npm-invokable song validator), closed by `npm run validate:songs`; the standing vestigial-BMS/dead-code cleanup pass was executed in the same increment (imported-song code path, idb `songs`/`audio` stores, wav.ts, loadFromBlob all removed). 604 unit tests green; typecheck/lint/build green; both e2e suites pass. Spec compliance is COMPLETE including every SHOULD, and no known dead code remains. UPDATE 2026-07-23: TWO new MUST-tier spec additions appeared as uncommitted diffs (playfield-rendering.md MUST 19 judgement-display time-axis alignment + 2 acceptance criteria; practice-mode.md MUST 15-18 lane shuffle + 3 acceptance criteria) and landed the same day as **Milestone 13** below (tag 0.0.21): MUST 19 was verified already-correct by construction and pinned with unit tests + a new e2e alignment probe; the practice lane shuffle is fully implemented (parse/undo/next-loop apply/HUD/e2e). 623 unit tests green; typecheck/lint/build green; both e2e suites pass. Spec compliance is COMPLETE again — every MUST and SHOULD across all specs is implemented.

**Scope change (2026-07-16): BMS import descoped.** The game ships built-in songs + practice mode only; user song import (BMS) is now a v1 non-goal. `specs/bms-import.md` was deleted and all import references were removed from the other specs (song-library.md is built-in-only, the IMPORT screen is gone from app-shell-navigation.md/song-select.md, chart-format.md drops the 'imported' audio source). The former Milestone 5 (BMS import pipeline) was removed and later milestones renumbered. The vestigial import support that survived the descope was fully removed in Milestone 12 (2026-07-22).

**Stack authority note:** The specs are authoritative on the stack — TypeScript + Vite + PixiJS + Web Audio, browser-only.

**Standard library convention:** `src/lib/` is the shared standard library. Cross-cutting utilities (chart types + validation, clock/time math, IndexedDB + localStorage wrappers, event bus, seeded RNG, audio helpers) are implemented there ONCE and consumed by feature modules. Feature code must not re-implement these.

**Newly authored specs (2026-07-14):** three gaps had no owning spec and were authored this iteration — `specs/app-shell-navigation.md` (screen state machine, bootstrap, audio unlock), `specs/settings-screen.md` (unified settings UI), `specs/builtin-song-content.md` (shipped songs, licensing, authoring pipeline). Plan items below trace to them.

---

## Milestone 0 — Scaffolding & tooling

- [x] Initialize Vite + TypeScript project (`package.json`, `tsconfig.json` strict mode, `vite.config.ts`), no UI framework — 00-overview.md
- [x] Add PixiJS dependency and a smoke-test canvas mount to confirm WebGL boot — 00-overview.md, playfield-rendering.md
- [x] Set up ESLint + Prettier (or Biome) for lint/format — 00-overview.md
- [x] Set up Vitest for headless unit tests (judgement/gauge/chart math must be testable with no DOM/render) — judgement-scoring.md, gauge-clear.md, chart-format.md
- [x] Establish directory layout: `src/lib/` (stdlib), `src/features/`, `src/app/` (shell), `public/songs/` (built-in assets) — 00-overview.md
- [x] Rewrite AGENTS.md: build (`npm run dev`/`build`), validation commands (`npm run test`/`typecheck`/`lint`), note specs override stack, remove Python/UV references — AGENTS.md
- [x] Add `.gitignore` for `node_modules/`, `dist/`; remove/ignore stale `.venv`, `.mypy_cache`, `.pytest_cache`, `.ruff_cache` — repo hygiene

## Milestone 1 — `src/lib` standard library (shared foundations)

- [x] `src/lib/chart/types.ts` — Song/Chart/Note/timing TypeScript types (formatVersion, difficulty enum, lane encoding 0=scratch/1-7=keys, tap|cn) — chart-format.md
- [x] `src/lib/chart/timing.ts` — beat→ms resolver accounting for BPM-change and STOP events; single shared computation reused by judgement/render — chart-format.md, playfield-rendering.md
- [x] `src/lib/chart/validate.ts` — load-time schema validation with clear field-path errors (reject lane out of range, negative beat, etc.) + migration-hook seam on formatVersion — chart-format.md (done; validate.test.ts added 2026-07-17 with the CN work — 18 tests incl. CN + span-overlap rules)
- [x] `src/lib/clock/audioClock.ts` — song-time conversion `songTimeMs = (ctx.currentTime − t0)×1000 − globalOffset + perSongOffset`; performance.now↔ctx.currentTime correspondence via `getOutputTimestamp()` with calibrated fallback — audio-playback.md, input-handling.md
- [x] `src/lib/storage/idb.ts` — IndexedDB wrapper for db `iidx-web` (stores: `songs`, `audio`, `practicePatterns`); atomic write helpers (commit metadata last), quota-error surfacing — practice-mode.md (`songs`/`audio` stores are vestigial after the 2026-07-16 BMS-import descope; only `practicePatterns` has a consumer going forward)
- [x] `src/lib/storage/local.ts` — typed localStorage wrapper with versioned JSON documents (records, settings, key map, play options) + corruption-safe read — results-records.md, input-handling.md, play-options.md, audio-playback.md
- [x] `src/lib/events/bus.ts` — typed event bus (judgement events → gauge/render, input events → judgement) — judgement-scoring.md, gauge-clear.md, playfield-rendering.md
- [x] `src/lib/rng/seeded.ts` — seeded RNG for RANDOM lane permutation (reproducible per roll) — play-options.md
- [x] `src/lib/audio/context.ts` — AudioContext singleton, user-gesture unlock, suspended-state handling, master/music/effect GainNode tiers, short-buffer SFX player — audio-playback.md
- [x] `src/lib/audio/wav.ts` — 16-bit PCM WAV encode + peak-normalize helpers (was for BMS mixdown; no feature consumer after the 2026-07-16 descope — candidate for removal)

**Learnings:**

- `timing.ts` control-point semantics: a BPM change at a beat applies before a same-beat STOP (the STOP duration uses the NEW bpm); a note exactly at a STOP's beat sounds when the STOP begins (excludes the STOP duration).
- Global offset is clamped to ±200ms in `audioClock`; formula `songTimeMs = (ctxNow − t0)×1000 − globalOffset + perSongOffset`.
- CN counts as 1 note in `noteCount()`.
- localStorage docs use an envelope `{version, data}` via `createLocalDoc`; keys `settings.v1`/`playOptions.v1`/`records.v1`.

## Milestone 2 — Minimal playable vertical slice (one built-in song, keyboard → judgement → render)

- [x] `src/app/` app shell + screen state machine (title → song-select → play → results); title screen doubles as first-gesture audio unlock; ESC semantics; bootstrap that restores localStorage settings — app-shell-navigation.md, 00-overview.md
- [x] Audio playback engine: single AudioBufferSourceNode per song, `t0 = currentTime + 1s` lead-in, scroll-in with no countdown, fade-out-then-stop on abandon/fail/complete, no residual nodes — audio-playback.md
- [x] Input system: keydown/keyup via `event.code`, ignore `event.repeat`, preserve `event.timeStamp`, per-lane independent 8-lane dispatch, `preventDefault()` on mapped keys during play only, default map (ShiftLeft, S D F Space J K L) — input-handling.md
- [x] Judgement engine (headless, unit-tested): δ windows (PGREAT 16.67 / GREAT 33.33 / GOOD 116.67 / BAD 250, tunable constants), earliest-unjudged-note-in-window consumption, miss POOR, empty POOR (no combo break), global offset applied — judgement-scoring.md
- [x] Score/combo aggregation: per-grade counts, empty POOR separate, combo/max-combo, FULL COMBO flag, EX score (PG×2+GR×1), theoretical max, DJ rank (AAA≥8/9…F), BP — judgement-scoring.md
- [x] Gauge engine (headless, unit-tested): 5 gauge types, recovery vs survival init/floor/clear-line, per-judgement constants table (balance-tunable), HARD <30% halving, EX-HARD no mitigation, immediate fail on survival 0% — gauge-clear.md
- [x] Playfield renderer (PixiJS): 1280×720 logical coords + aspect-fit + devicePixelRatio; scratch(wide)+7 lanes; IIDX colors; note y = `judgmentLineY − (noteBeat − currentBeat)×pixelsPerBeat×highSpeed`; currentBeat from audio clock (never rAF timestamp); STOP freeze; sprite pooling; culling; missed notes flow off-screen — playfield-rendering.md
- [x] HUD: gauge bar + clear-line marker (color-coded red/blue-green), combo, judgment text, EX score, progress bar, BPM, hi-speed readout, key beams, PGREAT flash — playfield-rendering.md, gauge-clear.md
- [x] Wire one hand-authored built-in chart JSON + ogg through the slice end-to-end; verify autoplay path — builtin-song-content.md, chart-format.md, playfield-rendering.md

**Learnings / decisions (Milestone 2):**

- Judgement window boundaries are INCLUSIVE with a 1e-6 ms float epsilon (±34ms → GOOD since 34 > 33.33); ±250 BAD, beyond → empty POOR.
- Empty POOR counts toward BP (IIDX convention) but not combo break and not into counts.POOR (missPoor only there).
- HARD gauge <30% mitigation halves ALL decrements, applies when pre-decrement value < 30 (exactly 30 → no halving).
- PlayResult/lamp: clearLampFor(type, cleared, fullCombo); FULL_COMBO overrides; NO_PLAY never returned by it.
- RESOLVED 2026-07-17: audio for built-in song was WAV not ogg (no vorbis encoder in env) — spec builtin-song-content MUST 9 deviation; resolved via a `wasm-media-encoders` WASM vorbis encoder (see the ogg learnings block).
- FNV-1a songId hash currently duplicated in scripts/generate-builtin-song.mjs and builtinCharts.test.ts — centralize into src/lib when Milestone 3 needs it for records. (Resolved in Milestone 3 via `src/lib/chart/ids.ts`; scripts/generate-builtin-song.mjs deliberately keeps an independent .mjs copy since the node script can't import TS.)
- Playwright is a devDependency used only by scripts/verify-e2e.mjs (not on build path); see .claude/skills/verify/SKILL.md for the headless-Chromium recipe incl. LD_LIBRARY_PATH workaround for missing system libs.
- Global menu keyboard handling lives in shell.ts as a single document-level keydown switched on machine.current(); PLAY-screen input is attached/detached by the session controller only.

## Milestone 3 — Results & local records

- Results screen skeleton already exists in shell.ts renderResults() — M3 adds records store, NEW RECORD badges, ±diff vs best.
- [x] Results screen: CLEAR/FAILED + gauge type, DJ rank, EX% of max, per-judgement counts, empty POOR, BP, max combo, options used, ±diff vs best, NEW RECORD badges, retry / return-to-select — results-records.md
- [x] Record store (localStorage, versioned JSON): key `songId+chartId`; clearLamp ordered enum (never downgrades; FULL COMBO overrides), bestExScore/bestRank (replace on improve), minBP, playCount, lastPlayedAt, arrangement-used flag; lamp/score update independently; write exactly once per completion; skip autoplay & practice — results-records.md, play-options.md
- [x] Corrupted-record recovery: no crash, prompt backup-then-reset — results-records.md
- [x] Deterministic songId (hash of title+artist) so rebuilt/re-added song assets reconnect to existing records — results-records.md, builtin-song-content.md

**Learnings / decisions (Milestone 3):**

- Abandoned/died plays (finishedSong=false) DO write playCount/lastPlayedAt and can set a FAILED lamp, but NEVER update bestExScore/bestRank/minBP — partial-run EX/BP is not comparable to a full play (documented in store.ts; resolves the former "Abandoned-play record policy" ambiguity).
- Autoplay writes nothing at all (not even playCount) — recordPlay returns null (play-options.md req 11).
- Record key is `${songId}::${chartId}`; doc shape `{version:1, data:{records:{[key]: ChartRecord}}}` at localStorage key `records.v1`.
- Arrangement is retained per-best: `lampArrangement` (play that set the lamp) and `bestExArrangement` (play that set bestExScore); `Arrangement` type lives in src/features/play/types.ts (single source, re-exported by records store). Gameplay RANDOM/MIRROR arrives in M5 (full play options) — schema is forward-compatible, no migration needed.
- Corruption recovery: on openRecordsStore, corrupt raw bytes are backed up to key `records.v1.corrupt` before the window.confirm reset prompt; declining leaves stored bytes untouched (next successful play overwrites them, backup preserved).
- `src/lib/chart/ids.ts` (fnv1a32/deriveSongId/deriveChartId, TextEncoder-based) is the canonical browser copy; scripts/generate-builtin-song.mjs keeps a deliberate independent .mjs copy (node script can't import TS) — ids.test.ts pins both to 'song-6f90aea6'.
- scripts/verify-records-e2e.mjs is a second Playwright e2e (same LD_LIBRARY_PATH recipe) asserting records.v1 contents in a real browser.
- Records export/import JSON + player statistics screen remain SHOULD items tracked in Milestone 7 (settings & polish); song-select lamp/best display is Milestone 4.

## Milestone 4 — Song library & song select

- [x] Song library: load `public/songs/index.json` metadata at start; lazy-load chart/audio at play time — song-library.md (as built, the catalog also merges IndexedDB imported songs; vestigial after the 2026-07-16 BMS-import descope)
- [x] Author built-in song content: ≥3 original songs across difficulty spectrum (incl. one BPM-change/STOP chart), index.json, chart validation script — builtin-song-content.md, song-library.md
- [x] Song-select screen (DOM): list (title/artist/genre/BPM min-max/source), expand to difficulty slots (level 1-12, note count), per-chart best record (lamp color/rank/EX), 3 sort modes (title/level/lamp) persisted, keyboard + mouse nav — song-select.md
- [x] Global play-options panel on select: gauge type, arrangement, hi-speed, SUDDEN+, autoplay entry; persisted globally across songs — song-select.md, play-options.md, gauge-clear.md
- [x] Load-failure handling: return to select with cause; missing-audio song does not crash — song-select.md, app-shell-navigation.md
- [x] Navigation entry point to practice from select (KeyP → PRACTICE_EDIT, hint updated) — song-select.md MUST 10 (landed with Milestone 6)
- [x] Navigation entry point to settings from select (KeyO → SETTINGS, hint updated) — song-select.md MUST 10 (landed with Milestone 7)

**Learnings / decisions (Milestone 4, partial):**

- Built-in songs are generated by `scripts/generate-builtin-song.mjs` + per-song modules in `scripts/builtin-songs/` (lib.mjs shared synth/WAV/id/beat-clock helpers). Three songs: "First Light" (Prism Unit, SYNTHPOP 150BPM, NORMAL ☆4 145n / HYPER ☆7 332n, no BPM change/STOP — MUST 4 baseline), "Neon Cascade" (Aurora Vector, ELECTRO, 140→175→140 with a 2-beat STOP ON the first BPM-change beat — MUST 3 showcase; NORMAL ☆6 193n / HYPER ☆8 331n, songId song-720e0160), "Overdrive Core" (Redline Theory, HARDCORE 185BPM, HYPER ☆9 470n / ANOTHER ☆11 710n, songId song-e09aa1a6).
- First Light's module intentionally keeps its own constant-BPM float math (NOT the generic beat clock) so regeneration stays byte-identical to the M2/M3 committed assets — verified via git status after regen. Don't "simplify" it onto createBeatClock: last-ulp float differences could shift samples.
- `scripts/builtin-songs/lib.mjs` createBeatClock mirrors src/lib/chart/timing.ts control-point semantics (same-beat BPM-change-before-STOP, note-at-stop-beat sounds at stop start) in seconds; audio events and chart notes come from the same event lists, so the showcase song is a real-usage validation of the timing pipeline. Neon Cascade charts include a scratch note exactly ON the stop beat; builtinCharts.test.ts pins this.
- builtinCharts.test.ts now asserts catalog SHAPE per builtin-song-content MUST 1-4 (≥3 songs, ≥2 slots each, low/mid/high bands, showcase + baseline timing charts, chart bpm metadata vs bpmEvents vs catalog range consistency) — 12 new tests, 286 total.
- verify-records-e2e.mjs was updated for the grouped select list (must navigateToSong('First Light') + Enter-to-expand before Enter-to-play; asserts exact songId/chartId now since 3 songs exist).
- Select-screen sort policies (recorded in model.ts): title=alphabetical; level=easiest chart ascending; lamp=weakest best-lamp first (grind order), tiebreak title. Selection tracked by id so it survives re-sort; Escape collapses difficulty rows (spec-compliant: app-shell-navigation.md deliberately omits SONG_SELECT from ESC list and TITLE is not a legal target).
- `select.v1` localStorage doc (sortMode) added to STORAGE_KEYS in src/lib/storage/local.ts.

## Milestone 5 — Full play options

- [x] Hi-speed 0.50–10.00 step 0.25, BPM-proportional scroll, in-play PageUp/PageDown, brief on-screen value, persisted — play-options.md
- [x] SUDDEN+ cover 0–80% step 1, Home toggle + Arrow up/down (repeat-on-hold), % white-number on cover, persisted — play-options.md
- [x] Arrangement OFF/RANDOM/MIRROR (scratch never affected) via `src/lib/rng`; pure lane substitution, no judgement/gauge effect; recorded on result — play-options.md
- [x] Autoplay demo (perfect play, gauge/judgement shown, record NOT saved) — play-options.md
- [x] Green-number visible-time readout (SHOULD); reference BPM pinned as MAX BPM (documented in spec) — play-options.md (done 2026-07-17; see learnings)
- [x] Extensible options-panel architecture seam for future LIFT / S-RANDOM — play-options.md (options.ts value-rules module + PlayControlAction union are the seam)

**Learnings / decisions (Milestone 5):**

- `src/features/play/options.ts` is the single source for option value rules (hi-speed 0.50–10.00 step 0.25, cover 0–80 step 1, arrangement cycle order); shell.ts panel and controller.ts in-play keys both consume it so the two surfaces can't drift.
- RANDOM re-roll scope RESOLVED: re-rolled per play attempt, retries included — shell.ts passes no `arrangementSeed`, so `startPlaySession` rolls fresh each time; `applyArrangement` runs ONCE pre-play so judge/renderer/autoplay all see the same substituted chart (judgement/gauge provably unaffected).
- In-play control codes (Escape/PageUp/PageDown/Home/ArrowUp/ArrowDown) take priority over lane keys so a custom key map can never shadow them (settings-screen.md MUST 10 reserves them); cover keys are the only ones that pass `event.repeat` through (hold-to-adjust, play-options.md MUST 6).
- SUDDEN+ white number only adjusts while the cover is ON (IIDX convention); the height is remembered across toggles. Default cover is 30% so the first toggle visibly covers something (spec leaves the default open).
- In-play hi-speed/SUDDEN+ changes persist by reporting FINAL values on PlayResult; shell.ts copies them back into playOptions.v1 in onFinished (play-options.md MUST 4/8).
- playOptions.v1 validator tolerates ABSENT new fields (older docs; read path spreads over defaults) but rejects present-and-mistyped ones — no version bump/migration needed.
- Option-change flash uses wall-clock `performance.now()` (pure UI); the audio clock stays reserved for judgement-relevant time.
- e2e guards the RANDOM path with a judgement-invariance check: RANDOM + autoplay must still produce PGREATs (catches lane-substitution breaking the judge).
- Green number (SHOULD 13, done 2026-07-17): formula is single-sourced in options.ts `greenNumberMs(bpm, hiSpeed, coverPercent, geometry)` with injected ScrollGeometry (keeps options.ts headless); render.ts exports `greenNumberFor` bound to RENDER_LAYOUT via a module-level hoisted geometry object (zero per-frame allocs, honoring the renderer's no-allocation contract). Reference BPM RESOLVED: select-screen GREEN uses the song's MAX BPM (catalog only carries a song-level bpm min/max before the chart JSON lazy-loads; max is the soflan-conservative choice), the in-play HUD GREEN line (PANEL_X/GREEN_Y 190, green fill) follows the live currentBpm each frame — spec play-options.md SHOULD 13 updated with the pinned decision.
- Select-bar GREEN follows the cursor: renderOptionsBar() reads the selected row's entry.bpm.max (folder rows → 'GREEN —') and renderSelectList() now calls renderOptionsBar() first, so every cursor/sort/filter/view re-render refreshes it. Cover only counts when suddenPlusEnabled.
- pixi.js imports CLEANLY in vitest's node env: options.test.ts imports RENDER_LAYOUT + greenNumberFor from render.ts (first test to import render.ts) as a geometry-drift guard pinning greenNumberFor(150,1,0)=1846. Retuning RENDER_LAYOUT must consciously update those pinned values AND the e2e GREEN assertions (1231/837/717 in verify-e2e.mjs).

## Milestone 6 — Practice mode

- [x] Grid pattern editor: 8 lanes × time axis, bars 1-8, snap 4/8/12/16/24/32, BPM 60-400, click/keyboard place/delete — practice-mode.md
- [x] Pattern = chart-format subset (tap only, no BPM/STOP events) — practice-mode.md, chart-format.md
- [x] Seamless loop playback with 1-bar (4-beat) metronome count-in scheduled on audio clock (strong/weak clicks, no setTimeout) — practice-mode.md, audio-playback.md
- [x] Reuse main judgement engine + renderer; no gauge; Hi-speed/SUDDEN+ apply; Escape → edit screen; transition ≤3s — practice-mode.md
- [x] Per-loop + cumulative stats (grade counts, accuracy %, signed delta avg); mid-session BPM change (next loop) — practice-mode.md
- [x] Save/list/load/delete named patterns in IndexedDB `practicePatterns` store — practice-mode.md
- [x] Preset patterns (trill/staircase/chords/scratch+key), target-loop auto-end + session summary (SHOULD 12/14) — practice-mode.md
- [x] δ histogram display (SHOULD 13) — practice-mode.md (per-loop/cumulative mean δ shipped; full distribution view deferred) (done 2026-07-17; see M6 learnings addendum)

**Learnings / decisions (Milestone 6):**

- Loop timeline (`src/features/practice/schedule.ts`): a session is chained cycles (1-bar count-in + pattern) with T[k+1] = T[k] + beats·60/bpm[k] — drift-free by construction (10-loop closed-form equality pinned in tests). Beats are BPM-independent (cycle k = session beats [k·beats,(k+1)·beats)) so BPM changes never move rendered notes; only judge/click TIMES change. BPM changes apply at the next cycle (locked LOOP_PREP_AHEAD_SEC=0.25s before the boundary).
- One judge per cycle on a shared session-ms axis; inputs route to the previous cycle's judge until LOOP_HANDOFF_MS=300ms past its end — provably unambiguous for BPM 60–400 and snap ≤1/32 (window overhang ≤231.25ms < 300 < 350ms ≤ next window start; proof pinned as a test). Judge note times are offset-free session ms; the SongClock applies the global offset to input times — same convention as song play, so offset semantics can't diverge.
- Metronome: `src/lib/audio/sfx.ts` (synthClickBuffer sine×exp-decay + createSfxScheduler, audio-playback.md MUST 8) scheduled at exact ctx times through effectsBus; strong 1720Hz on bar starts, weak 1080Hz elsewhere, clicks run through the pattern bars too (decision: spec only demands the count-in; a continuous metronome substitutes for the absent music). Clicks already in the past are skipped on catch-up after a rAF stall.
- Renderer reuse via `hud: 'practice'` init option in render.ts: gauge widgets simply aren't created (PlayFrameView.gauge is now nullable), an infoText multi-line stats block renders at RENDER_LAYOUT.INFO_X/Y instead. Practice controller rebuilds the info string only when a value changes.
- input.ts got an `extraControlCodes` option (base CONTROL_CODES always win conflicts) + `bpmUp`/`bpmDown` PlayControlActions (repeat-on-hold); practice maps Equal/Minus (PRACTICE_CONTROL_CODES). This is the extensibility seam noted in M5.
- Session size: targetLoops (editor LOOPS field, 0 = until-Escape) else floor(20000/notesPerLoop), capped at 200 loops — the renderer receives the whole session as one synthetic chart up front (loop-major note order; sessionIndex = loop·N + patternIndex).
- Practice writes NOTHING to records — controller.ts has no records import at all; e2e asserts records.v1 is byte-identical across a practice session. Accuracy in stats = EX% (comparable with song results); combo does not chain across loops (per-loop scorer, bestMaxCombo is best single loop).
- Editor (`src/features/practice/editor.ts`): DOM grid rendered bottom-up (beat 0 at the bottom); a cell covers [cellBeat, cellBeat+4/snap) so notes placed at a finer snap remain visible/deletable after a snap change (toggleCell removes everything in the cell). Editor owns a document keydown listener while active (attach/deactivate like the play session); focused inputs consume keys and Escape blurs them (app-shell-navigation.md MUST 17). Buttons blur after click so Space/Enter stay grid-owned.
- Patterns persist in the IndexedDB `practicePatterns` store (keyPath patternId) via `src/features/practice/store.ts`; corrupt rows are dropped on read by the isPracticePattern guard (list keeps valid ones).

## Milestone 7 — Settings, calibration, and polish

- [x] Unified settings screen (key-config UI, global offset, volumes, calibration entry; keyboard-operable) — settings-screen.md, input-handling.md, audio-playback.md (done: `src/features/settings/{model,screen}.ts`; KeyO entry from select)
- [x] Key-config UI: select lane → press key reassigns; reject duplicates showing conflicting lane; persist to localStorage; reset-to-default — input-handling.md, settings-screen.md (done: capture mode, conflict highlight, reserved-code rejection, per-lane + all-lane reset)
- [x] Global offset control −200..+200ms persisted; master/music/effect volume sliders persisted — audio-playback.md, settings-screen.md (done: live-apply via `gameAudio.setVolumes`; write-through `settings.v1`)
- [x] Offset calibration screen: BPM click tap capture → average error → proposed offset; seed from `outputLatency` (SHOULD) — audio-playback.md, settings-screen.md (done: in-settings modal; 16 taps at 120 BPM; outputLatency seed)
- [x] Song preview on select (loop highlighted excerpt) (SHOULD) — audio-playback.md, song-select.md (done 2026-07-17: src/features/select/preview.ts + shell wiring; see learnings)
- [x] Menu SFX (move/confirm/cancel) as short buffers — audio-playback.md MUST 8 (done 2026-07-17: `createMenuSfx` in src/lib/audio/sfx.ts + shell/settings wiring; see learnings)
- [x] CN (charge note) end-to-end: chart type 'cn'+endBeat, start=tap judgement, end release-in-window / early-release BAD, vertical connector render (all SHOULD) — chart-format.md, judgement-scoring.md, playfield-rendering.md (done 2026-07-17; see learnings below)
- [x] Dev overlays: FPS counter, keydown→judgement latency (SHOULD) — playfield-rendering.md, input-handling.md (done 2026-07-17: src/features/play/devOverlay.ts, F1 toggle; see learnings)
- [x] Player statistics + records export/import JSON (SHOULD) — results-records.md SHOULD 10/11, settings-screen.md SHOULD 13 (done 2026-07-17: in-settings RECORDS section — stats modal + export/import; src/features/records/{transfer,stats}.ts; see learnings)
- [x] Level folder view + title/artist search on select (SHOULD 11/13) — song-select.md (done 2026-07-17: view modes + filter in src/features/select/model.ts, '/' search box + F folder toggle in shell; see learnings)

**Learnings / decisions (Milestone 7, settings):**

- `src/features/settings/model.ts` is the headless interaction model (navigation cursor, capture mode, value mutations); screen.ts is presentation-only over it. The model mutates the SAME live `settings` object the shell passes to sessions — one source of truth is what makes "change offset then play immediately" work with no refresh (MUST 11). settings.v1 is written ONLY by the settings screen (write-through per change); the shell only reads + normalizes at boot (clampGlobalOffsetMs, clampVolume, isValidKeyMap fallback to defaults).
- keyMapLanes invariant: always a valid map (8 unique non-empty codes). Duplicate capture rejected with conflict lane highlighted; per-lane reset REFUSED when the lane's default code is currently bound to another lane (would silently invalidate the map); reset-all mutates the array in place (shell holds the reference).
- `RESERVED_LANE_CODES` is exported from input.ts derived from CONTROL_CODES, so settings-screen MUST 10 and the in-play control keys can never drift.
- Calibration is an in-settings MODAL, not a screen-machine state (resolves the spec's open question — app-shell-navigation.md MUST 1 has no calibration screen in the enum). It uses an offset-free SongClock; proposal = mean raw tap error because play-time δ = rawδ − globalOffset, so offset := mean(rawδ) centres the distribution. Taps predating the first click by more than half a period are ignored — e2e must wait past the 0.5s click lead-in before tapping (this bit the first e2e run).
- Sliders are built once and render() only updates values/classes — rebuilding an <input type=range> mid-drag kills the drag. Focused native widgets own keys per app-shell-navigation.md MUST 17 (Escape blurs); key capture is a model-level mode, not DOM focus, and eats every key first.
- Enter/any non-reserved code IS bindable to a lane (spec only reserves the six in-play codes); menu handlers don't consult the key map so this is safe.
- The screen keydown listener is attached on activate()/detached on deactivate() (practice-editor pattern); the shell's global keydown ignores SETTINGS entirely. DOM listeners added during dispatch of an event don't fire for that same event, so the KeyO that opens the screen can't leak into it.

**Learnings / decisions (Milestone 7, CN):**

- CN semantics (judgement.ts): the HEAD is the note's single scored judgement (CN = 1 note, maxEX = notes×2 — resolves the "CN note counting" ambiguity). A head hit parks the note in 'held' and opens a per-lane hold; release inside the end window (endTimeMs − good, inclusive w/ epsilon) → cnComplete (no scoring/gauge effect, silent on the HUD); earlier release → cnBreak ("treated as BAD": combo reset + gauge BAD delta + BP + kills FULL COMBO, but NOT added to grade counts — same extra-event precedent as emptyPoor). advance() auto-completes a still-held hold at endTimeMs, so holding through the end is never punished and the late window edge is unreachable by construction; a missed head never opens a hold (one note, one missPoor). Autoplay is press-only and relies on the auto-complete.
- JudgementKind gained 'cnBreak'/'cnComplete'; ScoreSummary gained cnBreakCount; gauge tables map cnBreak → BAD delta (HARD <30% halving applies naturally), cnComplete → 0. Shell/records/practice-stats needed no changes (they consume aggregated ScoreSummary fields only).
- validate.ts now enforces the CN span rule the judge relies on: on a lane, any note at beat <= an earlier CN's endBeat is rejected (INCLUSIVE — a note exactly at the tail would need an instant release+repress). scripts/builtin-songs/lib.mjs assertChartInvariants mirrors the same rule so generation fails loudly.
- Renderer (render.ts): CN body = pooled stretched sprite under the head chip (tail→head span, lane color at alpha 0.42 pending / 0.9 held / 0.2 broken-or-missed); while held the head chip pins to the judgement line and the body drains; a broken CN scrolls off dimmed with no chip. Cull cursor treats a CN as gone only when its TAIL passes the bottom edge.
- Autoplay stall bug found+fixed in controller.ts: after a rAF stall the catch-up loop can press two same-lane CNs in one frame, and the second onInput would replace the first still-active hold in the judge's activeHolds map, leaving it stuck 'held' (chip pinned at the line forever). Fix: drain judge.advance(noteTime) before each autoplay press (safe: all earlier notes already pressed, later notes outside the bad window).
- CN showcase content (builtin-song-content.md SHOULD 15): Neon Cascade only — the beats-60-64 riser charts as a held CN whose tail lands exactly ON the STOP beat (pins the same-beat rule for tails; builtinCharts.test.ts asserts it), and the outro's sustained 2-beat bass notes chart as CNs (NORMAL 194 notes / 5 CN, HYPER 333 / 9 CN). Zero audio changes — both sounds were already sustained, so the WAV is byte-identical; only chart JSONs + index.json changed. First Light untouched. verify-e2e.mjs Neon smoke extended to 30s (through the riser CN + STOP) asserting clean counts (BAD0/POOR0/BP0).
- Practice mode is deliberately CN-free (practice-mode.md MUST 4: tap-only patterns) — its controller ignores keyup for judging and doesn't map cnBreak/cnComplete to render states. Structurally unreachable today (pattern schema has no endBeat); if CN ever comes to practice, port the play controller's onRelease wiring + NOTE_HELD/NOTE_BROKEN mapping.

**Learnings / decisions (Milestone 7, song preview):**

- `src/features/select/preview.ts` is a headless preview player (injectable ctx/fetch/timers; 14 unit tests in preview.test.ts). Decided behavior for the one-line SHOULD: the preview follows the cursor's SONG (moving across a song's chart rows is a no-op), starts PREVIEW_DEBOUNCE_MS=300ms after the cursor settles (no churn while scrolling), fades in/out over PREVIEW_FADE_MS=250ms (song switches crossfade), loops the catalog excerpt seamlessly via AudioBufferSourceNode loopStart/loopEnd, and routes through musicBus (MUSIC volume tier governs it). `previewLoopBounds` clamps bad metadata (start beyond buffer → 0; invalid end → buffer end) so an empty/out-of-range loop is unrepresentable.
- Audio is fetched+decoded lazily on FIRST hover and cached per songId for the session — index.json stays the only boot-time fetch (song-library.md MUST 2 intent). A load-token guard prevents a slow fetch from starting audio for a song the cursor already left. Fetch/decode failure console.warns (NOT console.error — both e2e scripts fail on console.error), returns to idle, and clears the target so re-hovering retries; a broken preview can never crash select.
- Shell wiring is exactly two hooks calling one idempotent `updatePreview()`: the tail of renderSelectList() (every selection-changing path re-renders) and a second machine.onChange listener (leaving SONG_SELECT silences the preview; returning resumes it — settings/practice/results round-trips auto-resume from the decode cache). createScreenMachine supports multiple onChange listeners (handlers are a Set). Preview state mirrors into `data-preview-state`/`data-preview-song` attributes on the select screen root — verify-e2e.mjs gained a preview step asserting playing→follows-cursor→idle-in-settings→cache-resume.
- The catalog `preview: {startMs, durationMs}` field (builtin-song-content.md MUST 7) finally has its consumer; entries without a catalogEntry (imported, vestigial) simply never preview. Caveat: WAV audio means one preview hover fetches the full uncompressed song (~MBs) — fine at 3 songs, revisit if the catalog grows (the M2-learnings ogg-encoder item would also shrink this). (RESOLVED 2026-07-17: assets are now ~1MB ogg)

**Learnings / decisions (Milestone 7, menu SFX):**

- Menu SFX (audio-playback.md MUST 8) landed 2026-07-17 — this was the LAST unimplemented MUST across all specs; everything still open is SHOULD-tier. `createMenuSfx(ctx, destination)` in src/lib/audio/sfx.ts pre-synthesizes three buffers via the new `synthToneSequenceBuffer` (multi-segment sine, same 3ms-attack + exp-decay envelope per segment; `synthClickBuffer` now delegates to it as the single-segment case — byte-identical output, pinned by a delegation-invariant test). Palette: move = single 1568Hz tick (35ms, amp .35); confirm = rising 988→1319Hz; cancel = falling 659→494Hz (two-tone 50+90ms, amp .5) — musical pitches so rapid overlapping cues stay consonant.
- Wiring: the kit is created in shell.ts `unlockAudio()` on the effects bus (EFFECTS volume tier governs it, MUST 9); `playMenuSfx()` is null-safe (menus are unreachable before the title-gesture unlock, so pre-unlock = silence, never an error). Cue policy: move = cursor/sort/option-value changes (select screen + settings ↑/↓); confirm = Enter/row-click/title-gesture/entering settings-practice/results-retry; cancel = Escape paths (collapse only when it actually collapsed, results-back, settings-exit, practice-editor-exit — the exit cancels live in the shell's onExit callbacks so mouse paths sound too).
- Settings screen takes an optional `playMenuSfx` hook (move/confirm only); value adjustments (←/→) stay deliberately silent everywhere — volume rows already give live audible feedback (the effects preview click) and a tick on top would double-sound them. Capture mode and the calibration modal eat keys before the hook, so no cues fire during those.
- 465 unit tests green (8 new in sfx.test.ts: tone-sequence length/boundary/peak/delegation + menu kit reuse/distinctness/immediacy/cancelAll); both e2e scripts pass unchanged — menu cues fire throughout verify-e2e.mjs's full navigation and produce zero page errors.

**Learnings / decisions (Milestone 7, records stats & export/import):**

- The export file IS the persisted records.v1 envelope ({version:1, data}) — one schema for store and file (src/features/records/transfer.ts), so "import into a reset browser fully restores records" holds by construction. parseRecordsExport does strict per-field validation (field-path errors) because import text is an arbitrary user file; DJ_RANKS runtime list added to src/features/play/types.ts for it.
- Import into a browser WITH existing records = per-chart field-wise best-merge: lamp higher (winner's arrangement kept), bestExScore higher (winner's rank/arrangement kept), minBP lower, playCount MAX (not sum — sum would double-count on re-import; idempotency beats an approximate cross-device total), lastPlayedAt later. Pinned properties: idempotent + never downgrades local progress (transfer.test.ts). Store.importJson skips the write when nothing changed (stored bytes stay identical — e2e asserts this).
- SPECS UPDATED 2026-07-17 (Opus subagent): results-records.md SHOULD 10 now pins the merge semantics + a new acceptance criterion (double-import identical, import never lowers lamp/bestEx); SHOULD 11 + settings-screen.md MUST 1/SHOULD 13 now say stats/export/import live in a SHOULD-tier fifth RECORDS section of the settings screen (stats as an in-settings modal, calibration precedent) — app-shell-navigation's closed screen enum unchanged.
- stats.ts: cleared = lamp >= ASSIST_CLEAR; NO_PLAY bucket = library charts without a record; records for charts missing from the library still count in totalPlays/lamp distribution but not level rows (a record outlives its content). Aggregation is pure — takes RecordsData + StatsChartRef[] (library owns levels; ChartRecord doesn't store one).
- Settings screen: rows 14-16 (stats/exportRecords/importRecords) after calibration; stats modal eats all keys while open (Escape/Enter closes); model gained setNotice() so screen-owned outcomes (export/import results) surface through the one notice line; hidden file input data-role="import-records" (setInputFiles-able in e2e); onRecordsImported → shell renderSelectList() (select rows read records live, so one re-render refreshes lamps).
- BUG FIXED in screen.ts: smallButton handlers stopPropagation, so the li's focusing click handler never ran — the calibration START button (pre-existing) and the new VIEW/DOWNLOAD/CHOOSE buttons would have activated whichever row held the cursor. All four now setFocus(index) before performActivate(). Also URL.revokeObjectURL is deferred 30s — revoking synchronously can cancel the download the click just started.
- verify-records-e2e.mjs extended: needs `acceptDownloads: true` on newPage for the download event; steps cover stats modal aggregate, export file === stored records.v1, import-into-reset-browser restore, idempotent re-import (byte-identical), invalid-file rejection, imported lamp visible on select. Playwright pwlibs gotcha hit again: /tmp/pwlibs had been cleared — re-download per .claude/skills/verify/SKILL.md.

**Learnings / decisions (Milestone 7, select search & level folders):**

- model.ts was restructured for the two SHOULDs: selection is now a discriminated union ({song}|{chart}|{folder-level}) instead of two nullable id fields, SelectRow gained FolderRow (kind:'folder', no `entry` — anything consuming rows() must narrow on kind; shell's preview target and the tests' `.entry` accesses needed updating), and rows() dispatches on a `viewMode: 'song'|'level'`. Decisions: sort modes (S) order the SONG view only — the LEVEL view is fixed level-ascending with title-order inside a folder (the readout hides SORT there); one folder expanded at a time (mirrors the single-expanded-song rule); a selected CHART carries across view toggles with its container auto-expanded, any other selection re-anchors to the top; folders exist only for levels that have charts, so filtered-empty folders drop out and a stale expandedLevel is harmless.
- Search (SHOULD 11): query normalized trim+lowercase, case-insensitive substring on title OR artist, applied before row building in BOTH views. Deliberately session-only — persisting a filter would silently hide songs on the next boot. The '/' key focuses the box (preventDefault so the slash isn't typed into it); while focused the input owns the keyboard via stopPropagation on its own keydown listener (app-shell-navigation.md MUST 17), Escape/Enter blur it, and the filter itself stays active after blur (visible in the box + a "FILTER n SONGS / m CHARTS" readout on the sort line).
- select.v1 gained `viewMode` (persisted like sortMode; F toggles + saves). Validator follows the playOptions precedent: absent-is-ok (read path spreads over defaults), present-and-mistyped rejected — no version bump.
- e2e gotcha: an li's textContent concatenates child spans with NO separator ("LEVEL 4" + "1 chart" → "LEVEL 41 chart"), so assert against specific span selectors (.folder-title), never regex over the whole row text.
- 521 unit tests green (15 new in model.test.ts: filter matching/counts/selection-survival, folder grouping/expansion/wrap, view-toggle carry-over); verify-e2e.mjs gained two steps (search owns-keyboard + zero-match + restore; folder view grouping 4,6,7,8,9,11 + expand + select.v1 persistence round-trip) and verify-records-e2e.mjs passes unchanged.

**Learnings / decisions (Milestone 7, dev overlays + M6 δ histogram — 2026-07-17):**

- Dev overlay: `src/features/play/devOverlay.ts` is a headless metrics model (injectable timestamps, 12 unit tests). F1 was added to CONTROL_CODES in input.ts as `devOverlayToggle` — it therefore auto-joined RESERVED_LANE_CODES, so settings key-capture now rejects F1; specs updated same day (settings-screen.md MUST 10 + acceptance list F1; playfield-rendering.md SHOULD 16 and input-handling.md SHOULD 10 pin F1, the 500ms FPS window + worst-frame figure, and latency = performance.now() at end of judgement dispatch − event.timeStamp, last + session mean, real input only — autoplay records nothing since its onLane path is unreachable).
- Overlay visibility is a module-level page-session global (deliberately NOT persisted — a debug tool must never ship enabled via saved settings); metrics are per-session. Both play and practice controllers create their own instance; renderer shows it via a new optional PlayFrameView.devText mirrored into a pre-created Text node at RENDER_LAYOUT.DEV_X/DEV_Y (1050/230, both HUD modes, update-on-change, zero per-frame allocs).
- e2e can't read canvas text, so controllers mirror the overlay string onto the mount's `data-dev-overlay` attribute (same pattern as select's preview state); verify-e2e.mjs gained a step (toggle on → FPS figure appears after the 500ms window → real keypress produces INPUT/AVG lines → toggle off clears). Attribute is deleted on session cleanup.
- δ histogram (practice-mode.md SHOULD 13, now fully done): stats.ts gained a 15-bucket × 10ms (±75ms) accumulator — bucket edges are [lo,hi), center bucket covers [−5,+5), outliers clamp into edge buckets (edge buckets mean ≤−65 / ≥+65). Per-loop histogram on LoopStats, cumulative (finalized loops only) on CumulativeStats. Display is formatDeltaHistogram(): a one-line text sparkline (' ▁▂▃▄▅▆▇█', any nonzero bucket renders ≥▁) appended to the practice HUD infoText block — zero new render primitives. Spec updated same day to pin this presentation.
- 546 unit tests green after both; typecheck/lint/build green; both e2e scripts pass.

**Learnings / decisions (ogg vorbis conversion — 2026-07-17, builtin-song-content.md MUST 9):**


- Encoder: `wasm-media-encoders` devDependency; lib.mjs `encodeOggVorbisStereo` loads wasm via require.resolve (createOggEncoder()'s fetch() path doesn't work in node), configures channels 2 / 44.1kHz / vbrQuality 5 (~160kbps), and — critically — a FIXED oggSerialNo: libvorbis normally randomizes the ogg stream serial, and pinning it is what keeps `node scripts/generate-builtin-song.mjs` byte-identical across runs (verified: two consecutive regens hash-identical; chart JSONs untouched byte-for-byte, so records stay attached).
- encode() returns a view into wasm memory invalidated by the next call — every chunk must be copied (Uint8Array.from) before concat. PCM is fed in 128k-frame slabs.
- Song modules now return raw `pcm: {left, right, sampleRate}` and the generator encodes; `encodeWav16Stereo` was deleted from scripts/builtin-songs/lib.mjs (src/lib/audio/wav.ts remains the vestigial browser-side copy, still a cleanup candidate).
- builtinCharts.test.ts validates MUST 9 without a decoder: parses the Vorbis identification header (channels/sample rate) and walks ALL ogg pages (never string-search for 'OggS' — the pattern can occur in payload bytes) taking the final page's granule position as total PCM frames for an exact duration check (vorbis granules are sample-accurate). Peak (-1dBFS) is enforced at generation by normalizeAndAssertPeak instead — unverifiable bit-exactly from a lossy file.
- File sizes dropped ~12MB WAV → ~0.8-1.0MB ogg per song, which also resolves the M7 song-preview learnings caveat about full-file WAV fetches on hover.
- Vorbis decode is sample-accurate and gapless in decodeAudioData (no MP3-style priming-sample offset), so offsetMs semantics were untouched; judgement times come from chart timing anyway. Both e2e suites pass unchanged except assets.

## Milestone 8 — New MUST-tier features (2026-07-17 spec additions)

- [x] (C) Judgement explosion effect — playfield-rendering.md MUST 17 (done 2026-07-18; see learnings)
- [x] (B) Green-number lock / floating hi-speed — play-options.md MUST 15-17 (done 2026-07-18; see learnings)
- [x] (A) Scratch secondary key — input-handling.md MUST 12-14 + settings-screen.md MUST 15 (done 2026-07-18; see learnings)

**Learnings / decisions (Milestone 8 C+B — 2026-07-18):**

- Explosion (MUST 17): PlayFrameView gained per-lane `explosionAtMs` (Float64Array(8), init −Infinity — a zero init would flash every lane at t≈0) + `explosionPgreat` (Uint8Array). Both controllers set them inside dispatch() on `kind === 'hit' && grade PGREAT|GREAT` — the kind gate matters because cnComplete carries a cosmetic PGREAT grade that must NOT explode; kind 'hit' covers taps + CN heads, and autoplay funnels through the same dispatch so it needed no extra wiring. Renderer: fixed 16-sprite rig (per lane: rotated-diamond core + horizontal flare bar, blendMode 'add') in a new effectsContainer between cover and HUD (visible over any cover height, under judgement text/combo for readability, MUST 17); animation derives purely from age = songTimeMs − atMs (EXPLOSION_MS 260 < the 300ms budget; PGREAT flickers on the existing PGREAT_FLASH_MS cadence and is larger/brighter than GREAT's steady burst — the MUST 10 flash "extended", not replaced). One explosion per lane max (a new hit restarts it) ⇒ pooling + zero per-frame allocation by construction. No render unit tests exist (no render.test.ts precedent); verified at the browser surface — all autoplay e2e smokes render explosions with zero page errors.
- Green lock (MUST 15-17): inverse formula `hiSpeedForGreenTarget` lives in options.ts beside greenNumberMs (headless, geometry-injected), bound in render.ts as `lockedHiSpeedFor` beside greenNumberFor. The derived hi-speed is deliberately NOT snapped to the 0.25 grid (snapping would defeat "GREEN holds the target through soflan") but clamps to 0.50–10.00 (MUST 17); degenerate bpm/target ≤ 0 → HI_SPEED_MAX. Controllers: `greenLock` is a per-session const (ON/OFF is a select-panel decision; only the target adjusts in play), frame loop derives view.hiSpeed from live BPM + cover when locked while the manual `hiSpeed` variable stays untouched (and is what PlayResult.hiSpeed reports — e2e pins that a locked play leaves it byte-identical in playOptions.v1); onControl hiSpeedUp/Down adjusts the target ∓10 when locked (PageUp = faster = target DECREASES, MUST 16). PlayResult/PracticeOutcome gained greenLockEnabled/greenLockTargetMs; the shell copies the target back in onFinished/onExit (same persistence path as hiSpeed). playOptions.v1 gained both fields (absent-ok/present-mistyped-rejected validator precedent, no version bump; boot clamps the target onto the 10ms grid). Practice honors the lock too (decision: the "hi-speed behaves exactly like song play" precedent; practice BPM only changes at loop boundaries so re-derivation reacts there + on cover changes).
- Select panel: new G-LOCK span (`G-LOCK OFF` / `G-LOCK 880ms`); KeyL toggles it; enabling SEEDS the target from the current manual green at the cursor's max BPM (snapped to the grid) so the scroll feel doesn't jump the moment the lock engages (decision — the spec leaves seeding open); while locked, HI-SPEED shows `AUTO <derived@maxBPM>` and ←/→ adjust the target (→ = faster = −10ms, mirroring in-play PageUp), and GREEN always shows the ACTUAL value recomputed from the derived clamped hi-speed (= the target unless the clamp bit, MUST 17).
- e2e observability: the play controller mirrors the live HUD green number onto `data-green` on the PLAY mount (update-on-change, same pattern as data-dev-overlay; deleted on cleanup) — canvas text is unreadable to Playwright. verify-e2e.mjs gained a green-lock step: seed 900 from HS 1.75@175 → arrows to 880 → play Neon Cascade through the 140→175 change asserting data-green holds 880 on BOTH sides → in-play PageUp → 870 → persistence (greenLockEnabled true, target 870, manual hiSpeed untouched) → toggle-off restores the manual display. 554 unit tests green (8 new in options.test.ts: target grid/clamp rules, inverse round-trip, 140/175 soflan invariance, clamp-divergence, degenerate ceiling, renderer-bound pin); both e2e suites pass.

**Learnings / decisions (Milestone 8 A: scratch secondary key — 2026-07-18):**

- Storage/type design: `LaneKeyMap` gained an OPTIONAL `scratchSecondary?: string | null` field — NOT a 9th `lanes` entry, so `isValidKeyMap`'s 8-lane invariant and every positional consumer stay untouched; the function additionally requires a bound secondary to be non-empty and distinct from all lane codes (MUST 14: max 9 mutually-unique). settings.v1 gained `keyMapScratchSecondary: string | null` (absent-ok/mistyped-rejected validator precedent; boot normalization drops a colliding/empty/mistyped value to null). DEFAULT_KEY_MAP binds none (MUST 12).
- input.ts now tracks held state per-CODE (a `downCodes` Set): the keydown guard is per-code, so the secondary fires an independent lane-0 input while the primary is held (MUST 13 — the old per-lane `held[lane]` guard would have swallowed it); `held[lane]` remains "≥1 code down". `LaneKeyEvent` gained `laneHeld` (lane state AFTER the event; always true on downs) — both controllers mirror the key beam from it instead of `e.down`, which is what keeps the beam lit when one of two held keys releases.
- CN interaction DECISION: the play controller calls `judge.onRelease` only when `!e.laneHeld` — a hold releases when the LAST key leaves the lane, so tapping the other scratch key can never break an active CN (the MUST 13 beam rule extended to holds; spec is silent, and per-keyup release would make BSS-style holds unplayable with alternating fingers). Single-key behavior is provably identical (sole keyup ⇒ laneHeld false).
- Settings model: capture state gained a slot dimension (`capturingSlot(): 'primary'|'secondary'|null`; `capturingLane()` unchanged for existing consumers), plus `beginSecondaryCapture()`/`clearScratchSecondary()`. The duplicate scan covers all 9 codes in BOTH directions: a secondary capture rejects any lane's code, and a primary capture rejects the secondary's code (conflict highlights lane 0 — the row hosting the slot — and the notice names 'SCRATCH (2ND)'). `resetLane(0)` also clears the secondary (settings-screen MUST 15); `resetLane(1-7)` is REFUSED when that lane's default code is currently held by the secondary (same never-invalidate-the-map rationale as the lane-conflict refusal); `resetAllLanes` clears it. Row count stays 17 — model.test.ts pins the row layout, so the secondary is a second slot ON the lane-0 row, never a new row.
- Screen: lane-0 row gained a `.settings-value-secondary` span ('2ND: —' when empty) + '2ND' (capture) and '×2ND' (clear) buttons; keyboard: Insert = secondary capture, Shift+Delete = clear secondary only, plain Delete stays the full per-lane reset. The e2e `settingsValue()` helper was unaffected because $eval returns the FIRST `.settings-value` match (the primary span precedes the secondary).
- Verification: 570 unit tests green (10 new in model.test.ts for the slot rules, 6 new in input.test.ts incl. rapid-alternation and last-key-up beam semantics; 3 pre-existing exact-object onLane assertions gained the `laneHeld` field). verify-e2e.mjs gained: a settings step (assign/conflict-both-directions/reserved/clear), reload-persistence + reset-all-clears assertions, and an in-play MUST 13 acceptance that binds ShiftRight, turns autoplay OFF, holds ShiftLeft and taps ShiftRight mid-play, then asserts the dev overlay recorded EXACTLY 2 input samples — impossible under the old per-lane guard. Both e2e suites pass.

---

## Milestone 9 — FAST/SLOW timing indicator (2026-07-18 spec addition)

- [x] Judgement engine: classify every δ-based non-PGREAT judgement (GREAT/GOOD/BAD incl. CN start; excl. miss POOR / empty POOR / CN-break BAD) as FAST (δ<0) or SLOW (δ>0) using the same offset-adjusted δ as the grade; include δ + classification in judgement events; aggregate per-session FAST/SLOW counts regardless of display mode — judgement-scoring.md MUST 14-16
- [x] Play options: timing display mode OFF / FAST-SLOW / ±ms, default FAST/SLOW, set in the select options panel only (no in-play key), persisted like other options; display-only — no effect on judgement/gauge/score/records — play-options.md MUST 18
- [x] Renderer: when mode ≠ OFF, draw `FAST` (blue) / `SLOW` (red) text or signed δ ms at a fixed spot adjacent to the judgement text; same lifetime as the judgement text, replaced on the next judgement, cleared by unclassified judgements (PGREAT/miss POOR/empty POOR); works in practice too; reused text objects (MUST 12 no-per-frame-alloc contract) — playfield-rendering.md MUST 18
- [x] Results screen: always show FAST/SLOW counts alongside the per-grade counts; records.v1 schema stays unchanged (display-only, never persisted) — results-records.md MUST 13

**Learnings / decisions (Milestone 9 — 2026-07-18, tag 0.0.17):**

- Event contract: `JudgementEvent` gained a REQUIRED `timing: TimingClass | null` ('FAST'|'SLOW', types.ts) set by `timingClassFor(grade, deltaMs)` exported from judgement.ts — the single classifier source; the scorer and every display surface consume the event field and never re-derive the δ sign (scoring.test.ts/stats.test.ts factories also call timingClassFor so tests can't drift). All non-hit kinds hard-code `timing: null`; a cnBreak's BAD is deliberately unclassified (not δ-based, MUST 14 excludes it); δ exactly 0 classifies as neither (unreachable for non-PGREAT grades under any real window set, but the rule is total). CN heads classify like taps because they are `kind: 'hit'`.
- Aggregation: `ScoreSummary` gained `fastCount`/`slowCount`, counted in the scorer's hit branch from `event.timing`. Mode-independence (MUST 16: OFF still aggregates) holds by construction — the scorer has no notion of the display option, pinned by a test. `PlayResult` needed NO new fields (results reads `result.score.fastCount`); records.v1 untouched by construction (recordPlay copies scalar fields only — MUST 13 schema-invariance is free).
- Option plumbing: `TimingDisplayMode` ('OFF'|'FAST_SLOW'|'MS') lives in types.ts beside Arrangement; `TIMING_DISPLAY_MODES` (cycle starts at the FAST_SLOW default), `nextTimingDisplay`, `timingDisplayLabel` ('FAST/SLOW'/'±ms'/'OFF') in options.ts. playOptions.v1 gained `timingDisplay` (absent-ok/present-mistyped-rejected validator precedent, no version bump). Select-panel KeyT cycles it (TIMING span between SUDDEN+ and AUTOPLAY); deliberately NO in-play key (MUST 18) and NO copy-back from PlayResult in onFinished — same policy as gaugeType/arrangement/autoplay, which also have no in-play keys.
- Renderer: the mode is a `PlayRenderInit` option, not a PlayFrameView field — it's fixed per session, unlike suddenPlus/optionFlash which ride the view because they change mid-play. `view.lastJudgement` gained `timing`/`deltaMs`. One reused `timingText` at TIMING_TEXT_Y 402 (below judgement text 360, clear of option flash 622), fontSize 22; FAST = 0x3fa7ff (the even-key blue), SLOW = 0xff4455 (the POOR red). Visibility shares the judgement text's JUDGEMENT_HOLD_MS window; the rebuild cache keys on (atSongTimeMs, deltaMs) — two numeric compares per frame, string built only on a new classified judgement (zero-alloc contract). In song play cnComplete doesn't replace lastJudgement, so a completed CN leaves the indicator up exactly like it leaves the judgement text.
- e2e mirror: `formatTimingIndicator(mode, timing, deltaMs)` exported from render.ts ('FAST'/'SLOW' word or signed integer '±Nms') is used by BOTH the canvas and the controllers' `data-timing` dataset mirror, so the two can't diverge on format; the mirror re-states the 3-line visibility rule against the same RENDER_LAYOUT.JUDGEMENT_HOLD_MS. Both controllers (play + practice) mirror and delete data-timing on cleanup; practice receives the mode from the same playOptions doc.
- e2e technique (verify-e2e.mjs): the indicator lives only 500ms per judgement so poll reads race it — the new steps arm a MutationObserver on data-timing that records every non-empty value shown (`window.__timingSeen`; disconnect the previous observer before re-arming, else old closures keep writing into the new array via the window lookup). Real-input strategy: round-robin mash over all 8 lane keys — a full cycle is shorter than the ±250ms bad window, so any passing note is consumed by SOME press, and mash-timed consumption is essentially never PGREAT ⇒ a classified display is deterministic in practice (observed: mash always leads the notes — all FAST, e.g. '-233ms' in ±ms mode). Autoplay results now assert FAST0SLOW0 (acceptance: perfect play classifies nothing) and the Neon CN clean-count EXACT grid string gained FAST0SLOW0 — that string must be updated whenever results rows change.
- 587 unit tests green (17 new: judgement classification, scorer aggregation, option cycle/labels, formatter); typecheck/lint/build green; both e2e suites pass (verify-e2e gained 4 steps: panel cycle/persistence, FAST/SLOW-mode real-input display + results counts, ±ms-mode display, OFF-mode never-displays-but-still-aggregates).

---

## Milestone 10 — Practice song content (2026-07-21, specs/practice-song-content.md)

- [x] Built-in practice song "Chord Dojo 282" (Prism Unit, genre PRACTICE, songId song-19d6fdce): single ANOTHER ☆12 chart, 182 notes (158 key + 24 scratch), 16 bars / 64 beats @ fixed BPM 282, tap-only, total 189.1 — MUST 1-6, SHOULD 13
- [x] Keysound asset + playback: keysound.ogg (120ms, vorbis 44.1kHz stereo) played on every lane press through the MUSIC bus, preloaded at song decision — MUST 7-8, 10
- [x] No-BGM master clock: t0 reservation with no source node, natural end at last note + 2s through the normal completion path — MUST 9
- [x] Catalog/loader keysound entry support (audio/preview replaced by keysound) — MUST 11
- [x] Chart passes validate.ts + builtinCharts.test.ts coverage incl. section-structure pinning — MUST 12
- [x] Practice presets dojo-chords / dojo-scratch (4-bar A/C excerpts, preset-carried BPM 282) — SHOULD 14

**Learnings / decisions (Milestone 10 — 2026-07-21, tag 0.0.18):**

- Pattern authored from the spec's MUST 4-5 structure rules (reference image unavailable — MUST 6's explicit fallback): A chord-stream bars 1-9 (2-4-key chord every quarter beat, adjacent chords always differ, scratch on bar starts of bars 1/3/5/7/9 + bar 5 beats 1·3), B break bars 10-12 (bar-start scratch+chord, 8th-note singles between), C scratch-rush bars 13-16 (scratch + 2-3-key chord on 15 of 16 beats; beat 55 deliberately scratch-free for "거의 매 박"). Exact tables live in scripts/builtin-songs/chord-dojo.mjs with generation-time asserts (A adjacency, ≥3 scratch+chord beats per C bar) mirrored as unit tests — pattern drift fails generation AND the suite.
- Catalog schema: `CatalogSongEntry.audio` is now OPTIONAL with a new optional `keysound`; parseSongEntry enforces exactly-one-of and rejects preview on keysound entries (so "no select preview" is structural). `Song.audio` is KEPT — ref simply points at the keysound URL, offsetMs 0 (chart-format.md MUST 8 amended to say so; validateSong untouched). PlayableSong AudioSource gained `{kind:'keysound', url}`; shell decodes it via player.loadFromUrl at song decision (MUST 10 preload) and passes the new `PlaySessionAudio` union (`{kind:'bgm'|'keysound', buffer}` — replaced PlaySessionOptions.audioBuffer).
- Engine: keysound mode uses `createSilentPlayback` (songPlayer.ts — t0 = ctx.currentTime + DEFAULT_LEAD_IN_SEC, isActive until stop(), ended resolves only via stop) and durationMs = last note (CN tails included) + 2000; natural end rides the EXISTING frame-loop `t > durationMs + RESULT_DELAY_MS` branch (results ~2.3s after the last note; the BGM ended.then straggler path is a no-op here since ended only resolves inside endSession).
- Keysound trigger policy (src/features/play/keysound.ts): fires on kinds 'hit' | 'emptyPoor' ONLY — the two press-derived judge events; missPoor/cnBreak/cnComplete are advance/release-derived and never sound. Wired as `keysound?.onJudgement(event)` at the top of controller dispatch(), so real input AND autoplay presses are both audible (decision: IIDX autoplay plays keysounds; spec silent). cancelAll() in cleanup keeps the no-residual-nodes acceptance. Type plumbing: createSfxScheduler's ctx param narrowed to new `SfxScheduleContextLike` (currentTime + createBufferSource) and SfxBufferSourceLike.connect's return loosened to unknown, so the controller's SongAudioContextLike builds the scheduler with zero casts.
- Keysound synth: 120ms two-partial pluck (1046.5 + 0.5×2093 Hz, 3ms attack, 30ms exp decay, 5ms end fade), peak-normalized to 0.18 linear NOT the song's −1dBFS: up to 5 sample-aligned instances (scratch + 4-key chord) sum to 0.9 — the −1dBFS target would hard-clip on every big chord. Generator writes `song.audioFilename ?? 'audio.ogg'` (chord-dojo returns 'keysound.ogg'); double-run regen byte-identical, existing 3 songs untouched.
- Presets: `PracticePreset` gained optional `bpm` (editor applyPreset applies it via setBpm and syncs the BPM input — excerpts only make sense at source tempo); dojo-chords (43 notes/4 bars) and dojo-scratch (51) tile their 16-beat excerpt and truncate at bars×4. builtinCharts.test.ts pins preset build(4) === chart excerpt (bars 1-4 / bars 13-16 rebased −48) so the two copies cannot drift.
- builtinCharts.test.ts restructure: per-song audio checks branch on `audio ?? keysound` (keysound: ogg vorbis 44.1kHz stereo + ≤150ms; the chart-fits-in-audio check is BGM-only — session length is lastNote+2s by construction); MUST 1-2 + level-band coverage filter to `bgmSongs` (the practice song must not carry them); new practice-song describe pins identity/genre/1-slot/offsetMs-0/license, the 282×16bar tap-only shape, A/B/C structure, adjacency, and the C-bar acceptance criterion. 619 unit tests total.
- e2e: verify-e2e.mjs gained 3 practice-song steps — 1-slot select shape; autoplay natural-end run asserting the ONLY song-19d6fdce requests are chart-another.json + keysound.ogg (no BGM fetch), PGREAT182 / EX 364/364 / FULL COMBO, records untouched; real-input mash + abandon writes playCount via the normal records path. The preview step now also pins the dojo row staying preview-idle. Select-list assumptions updated (4 songs; folders 4,6,7,8,9,11,12; Chord Dojo sorts first under TITLE).
- **e2e FLAKE FIXED (pre-existing, exposed by environment speed):** the FAST/SLOW / ±ms / OFF steps' full-speed 8-lane round-robin mash cleared data-timing within ~12ms of every classified hit — the NEXT press's empty POOR correctly replaces the indicator (MUST 18) — faster than the mirror's rAF sampling, so the MutationObserver could see nothing while the results grid counted FAST hits (diagnosed via a minimal repro: grid FAST4, seen []). mashAllLanes now paces 60ms/press: cycle 480ms still < the ±250ms window (every passing note consumed) and the display window exceeds the frame period even at degraded headless fps (always sampled).
- **KNOWN FLAKE (1× occurrence, unreproduced):** verify-records-e2e 'retry abandon' once froze on PLAY with an initial-state HUD (gauge 22%/EX 0, notes never appeared, Escape dead) — symptoms fit the retry session's AudioContext/source.onended stalling in headless swiftshader (playback.stop() promise pending ⇒ cleanup/onFinished never ran). Passed on immediate re-run AND in a dedicated instrumented repro (rAF + input both live). Code path untouched by M10; watch for recurrence before chasing.
- Spec reconciliation (Opus subagent, same day): builtin-song-content's exception paragraph now carves out the keysound.ogg filename from MUST 6; chart-format MUST 8 states the practice song KEEPS the song-level audio reference (→ keysound sample) and practice-song-content's exception list dropped it; song-library MUST 3 excludes the practice song from its 3-song count; song-select SHOULD 12 pins no-preview songs silent; audio-playback's exception paragraph pins perSongOffset 0.

---

## Milestone 11 — δ-histogram results display, gauge-death presentation, clock calibration guard (2026-07-22, tag 0.0.19)

- [x] Results-screen δ-histogram sparkline + accumulator moved into the scorer (judgement-scoring.md SHOULD 13; spec updated to pin bucket definition + presentation)
- [x] Gauge-out death freeze + fade before FAILED results (playfield-rendering.md SHOULD 15)
- [x] Master-clock fix: plausibility guard on `getOutputTimestamp()` calibration (audio-playback.md MUST 6, amended)

**Learnings / decisions (Milestone 11 — 2026-07-22):**

- δ histogram single-source: the 15×10ms ±75ms accumulator moved from practice/stats.ts into the Scorer (`scoring.ts` exports DELTA_HISTOGRAM_BUCKETS/BUCKET_MS + formatDeltaHistogram; ScoreSummary gained `deltaHistogram`), so song play and practice (one Scorer per loop) share one implementation — practice stats now just merges finalized loops' summaries. Results screen renders the same text sparkline in a `.result-histogram` div under the count grid; empty (no timed hits) renders nothing. PGREATs ARE bucketed (unlike FAST/SLOW which excludes them); non-hit kinds carry `deltaMs: null` and are skipped. Display-only — records.v1 untouched by construction.
- Death presentation (SHOULD 15): on `gauge.failed()` the controller freezes the playfield by early-returning from frame() before any song-time-driven view field updates — the death-detecting frame still populates the view once, so the fatal judgement + 0.0% gauge land on screen and stay. Wall-clock driven fade (DEATH_FADE_DELAY_MS 180 → linear DEATH_FADE_MS 700; DEATH_HOLD_MS 1000 total before results, concurrent with the 300ms audio fade inside playback.stop()). Renderer: one pre-created full-screen black sprite ABOVE the HUD (whole frame sinks together); alpha comes from the controller (`view.deathFadeAlpha`). Survival gauges only — recovery gauges cannot fail mid-song. e2e: controller mirrors `data-death` onto the PLAY mount the frame the gauge dies; verify-e2e.mjs plays First Light NORMAL on HARD with autoplay OFF (12 misses drain it) and asserts the hold lands in [600ms, 4s] plus 'HARD GAUGE'/'died at'/GAUGE0.0% on results.
- **CLOCK BUG (real, judgement-breaking in headless): `calibrateSkewSec` trusted any `getOutputTimestamp()` with finite fields and contextTime > 0.** In headless Chromium (no audio sink) the output timestamp advances SLOWER than currentTime — the implied perf↔ctx skew drifts ~140ms per second of context life, unboundedly (measured via a browser repro: diff −16ms at 0.5s → −419ms at 3s of context age). Skew is calibrated lazily at first input of each session, so the longer the page had been open, the further ALL real inputs mapped from song time — by the FAST/SLOW e2e step every press was seconds off → 100% empty POORs, 0 notes consumed (diagnosed from grid: POOR(miss)8/empty90/others 0). Fix in audioClock.ts: always compute the sync-sampled fallback skew; accept the output-timestamp skew only when the implied output latency (fallbackSkew − outputSkew) is within [−20ms, +350ms] (real device range: ≈0 wired, ~300ms Bluetooth; output can never lead currentTime). Within range the OUTPUT skew still wins — it judges inputs against the audio the player hears (latency-compensated). audio-playback.md MUST 6 amended to pin the guard; 3 new + 2 corrected unit tests (stalled pipeline, output-ahead, Bluetooth-class 300ms accepted, existing fakes given sync sources consistent with plausible latency).
- Retroactive insight: M9's e2e observation "mash always leads the notes — all FAST, e.g. −233ms" was THIS clock bias (near the ±250ms window edge), not player timing — the M10 "pacing fix" worked around a partially-broken clock, and environment drift growth eventually pushed it past the window entirely (reproducibly, not flaky). Post-fix, mash produces a genuine FAST/SLOW mix with deltas across the window and zero miss POORs during consumption. The M10 "KNOWN FLAKE" (retry-abandon PLAY freeze) remains separate/unreproduced — unchanged this round.
- e2e note: the autoplay results step now also asserts the histogram is a lone full-height center column (δ=0 exactly), pinned as `/^δ −75ms {8}█ {8}\+75ms$/`.

---

## Milestone 12 — Vestigial-BMS cleanup + standalone song validator (2026-07-22, tag 0.0.20)

- [x] Removed the descoped BMS-import code path end-to-end (library merge, `source: 'imported'`, idb `songs`/`audio` stores, `wav.ts`, `loadFromBlob`, IMPORTED badge) + all dead tests
- [x] builtin-song-content.md SHOULD 14: `scripts/validate-songs.ts` + `npm run validate:songs` (the last unimplemented SHOULD anywhere — spec compliance now includes every MUST and SHOULD)

**Learnings / decisions (Milestone 12 — 2026-07-22):**

- library.ts is builtin-only now: `LibraryEntry` lost `source` (the select-row BUILT-IN/IMPORTED badge and its CSS were removed — song-select.md no longer asks for a source display), `catalogEntry` became REQUIRED (was "present when builtin"), and `SongLibrary` lost `warnings` (the IDB merge was its only producer; shell's warnings display line removed). `AudioSource` lost the `blob` variant and `LibraryDeps` lost `db`; `loadPlayableSong` no longer takes a db. songPlayer lost `loadFromBlob` (interface + impl + test).
- idb.ts now creates ONLY `practicePatterns`. `DB_VERSION` stays 1 DELIBERATELY: `onupgradeneeded` fires only on a version bump, so a profile from a pre-cleanup build keeps empty `songs`/`audio` stores (harmless, documented in the header); a fresh profile gets just `practicePatterns`. Deleted: `putSongAtomic`, `deleteSong`, `idbGetAllKeys`, and `idbPut`'s out-of-line `key` param (all had zero production consumers). `StorageQuotaError` KEPT — still live via the practicePatterns write path. idb.test.ts was retargeted to pattern-shaped rows.
- `SongAudio.source` narrowed to `'builtin'` in types.ts/validate.ts (chart-format.md had already dropped `'imported'` from the union at descope time — code was lagging the spec).
- `src/lib/audio/wav.ts` + `wav.test.ts` deleted (no consumer since the 2026-07-17 ogg conversion); `scripts/builtin-songs/lib.mjs` keeps its own independent `peakNormalize` (its comment no longer cites the deleted file).
- validate:songs (SHOULD 14) runs via vite-node (ships with vitest) so the script imports the app's REAL TS modules — `loadBuiltinCatalog`/`loadBuiltinSong` (catalog parse + `loadChart` schema validation), `computeNoteTimesMs`, `noteCount`, `deriveSongId`/`deriveChartId` — zero duplicated validation logic, so the script cannot drift from the app's load path. Checks per catalog slot: chart loads + schema-validates, noteCount/difficulty/level match index.json, chartId/songId match their deterministic derivations (a drifted ID would orphan local records), audio/keysound file exists on disk, beat→ms yields finite times. Verified both directions: OK on current assets; exit 1 with a field-named error on an injected noteCount mismatch. The Vitest suite (builtinCharts.test.ts) still covers MUST 12 independently.
- 604 unit tests green (627 − 24 dead-path tests + 1 new keysound loadPlayableSong test); typecheck/lint/build green; BOTH e2e suites pass byte-unchanged — no e2e script referenced any dead path, exactly as the pre-removal inventory predicted.
- Process note: the removal was driven by a 6-subagent audit (5 spec-compliance + 1 dead-code inventory with file:line spans and full consumer lists). The inventory approach caught two subtleties worth repeating: (1) `idb.test.ts`'s `source: 'imported'` fixture was incidental, not evidence of a live feature; (2) verify-records-e2e's "imported lamp" step is about RECORDS import (transfer.ts), not song import — do not confuse the two features when grepping.

## Milestone 13 — Render/judgement alignment pin + practice lane shuffle (2026-07-23, tag 0.0.21)

- [x] playfield-rendering.md MUST 19 (judgement-display time-axis alignment): verified ALREADY CORRECT by construction, then pinned — unit tests + data-align e2e probe (no behavior change needed)
- [x] practice-mode.md MUST 15-18 (lane shuffle): 7-digit permutation entry, validation with reasons, next-loop apply, undo stack, HUD display, session-only reset — full implementation + unit tests + 5 e2e steps

**Learnings / decisions (Milestone 13 — 2026-07-23):**

- MUST 19 needed NO fix: both play and practice controllers drive render (`clock.songTimeMs()`) and judgement (`clock.eventTimeToSongTimeMs()`) from the same SongClock, and both conversions apply `−globalOffset+perSongOffset` identically (audioClock.ts:141-150), so the offset cancels symmetrically — a δ=0 input coincides with the note at the judgement line for any offset/hi-speed/green-lock/cover. Pinned by new audioClock.test.ts axis-agreement tests (offsets −60/0/+60 + mid-session change) whose comments spell out the reduction: axes agree ⇒ currentBeat = msToBeat(noteTime) = note.beat ⇒ y = judgementLineY.
- e2e observability for MUST 19: the play controller now records `misalignMs = judgeInputTime − view.songTimeMs` per real keydown (session min/max mirrored as `data-align="min:max"`, deleted on cleanup). Correct axes ⇒ min ≈ [0, one frame]; a render axis that lost/double-applied the offset shifts every sample by exactly ∓offset. The e2e step seeds globalOffsetMs 60 via localStorage + reload, mashes First Light NORMAL 4s, and bounds min ∈ (−25, +30) — the session MIN concentrates near the true axis gap regardless of headless fps, so the assertion doesn't flake on slow frames.
- **Output-latency compensation insight (important for future timing work):** the input axis DELIBERATELY differs from the render axis by the device output latency — `getOutputTimestamp` maps input events onto the audio the player HEARS, so on real hardware data-align min ≈ −latency (0 wired, ~−300ms Bluetooth) and visuals lead audio by that latency (inherent AV mismatch; judgement follows audio; the M11 plausibility guard bounds it to ≤350ms). In headless the stalled pipeline fakes a context-age-dependent ~150ms "latency" that the M11 guard rightly ACCEPTS while the context is young (it only rejects >350ms) — this is what the first alignment-probe run measured (−154ms) and it also explains the practice-summary "δ −153.6ms early" seen in headless e2e; neither is an app bug. The e2e alignment step therefore pins the sync-sampled skew path (unbiased by construction) by removing `AudioContext.prototype.getOutputTimestamp` via `page.addInitScript` before its reload — offset symmetry is identical in both calibration paths, so nothing real is untested.
- Shuffle module (`src/features/practice/shuffle.ts`, pure, 11 unit tests): `parseShuffleMapping` (trim; exactly 7 chars; digits 1-7; no duplicates; HUD-ready reason strings), `shuffleLaneMap` (original→display map, SAME shape as arrange.ts laneMapFor so it plugs into the same substitution sites; '7654321' ≡ MIRROR pinned against laneMapFor), `createShuffleState` (stack; current() = top ?? '1234567'; undo() pops, false at bottom). Mappings are ABSOLUTE (always vs the pattern's original lanes, per the spec's "원본 d번 레인" wording), never composed.
- Controller integration mirrors the pendingBpm pattern exactly: `prepCycle()` freezes `shuffle.current()` into `cycleShuffles[k]` and builds that cycle's judge from `cycleJudgeNotes(cycle, patternNotes, laneMap)` (new optional param — lanes substituted, times untouched, so judgement/stats invariance is by construction; pinned by a dual-judge unit test driving identical timings through plain vs shuffled judges). `remapUnlockedSessionNotes()` (called on apply/undo) mutates `sessionNotes[k·N…]` lanes in place for every NOT-yet-locked loop — the renderer re-reads `note.lane` from the same array every frame (render.ts never bakes lanes), so upcoming loops' notes already on screen switch immediately while locked cycles keep the lanes their judges were built with = "applies from the next loop". Same 0.25s prep-lock caveat as BPM (an apply within LOOP_PREP_AHEAD_SEC of a boundary lands one loop later).
- Entry UI is a DOM overlay (`.practice-shuffle` fixed top-right on the practice mount: input + APPLY/UNDO buttons + notice span), NOT control-code digits — the acceptance criteria demand rejecting arbitrary text like '123456A', which digit control codes cannot even represent. F2 (`shuffleEntry`) focuses the input, F3 (`shuffleUndo`) undoes; both are PRACTICE_CONTROL_CODES extras (the Equal/Minus precedent: they shadow a custom lane binding of the same code only inside practice). Escape in the box blurs (session NOT quit); Enter applies; rejection keeps focus for in-place correction; applying the already-current mapping is a no-op notice (pushing it would make undo look broken).
- input.ts gained a text-entry guard: keydowns whose event.target is INPUT/TEXTAREA/SELECT are ignored entirely (no lane/control dispatch, no preventDefault — app-shell-navigation.md MUST 17 extended into running sessions). Keyups are deliberately NOT guarded: a lane key released after focus moved into the widget must still clear held state or the beam sticks. `KeyLikeEvent` gained optional `target` (absent in test fakes ⇒ guard inert).
- HUD: `SHUFFLE <mapping>` line always in the practice info block (+ `NEXT LOOP SHUFFLE <pending>` when different, mirroring NEXT LOOP BPM); e2e mirror `data-shuffle="<currentLoop's>|<pending>"`. Session-only reset (MUST 18) holds by construction — PracticeOutcome carries no mapping and startPractice threads none in.
- verify-e2e.mjs: 5 new shuffle steps (apply-at-next-loop via data-shuffle transition; 3 rejection cases keep mapping; F3 undo to identity + NOTHING TO UNDO; F2 keyboard entry + Escape-blurs-not-quits; fresh session starts identity) + the MUST 19 alignment step at the end (leaves stored offset back at 0).
- Pre-existing lint break fixed in passing: scripts/validate-songs.ts `let songCharts;` violated noImplicitAnyLet (annotated with `Awaited<ReturnType<typeof loadBuiltinSong>>['charts']`) — `npm run lint` was failing on the committed tree.
- 623 unit tests green (+19: 11 shuffle, 3 input guard, 2 audioClock axis-agreement, 1 schedule laneMap, plus reorganized asserts); typecheck/lint/build green; both e2e suites pass.

## Key ambiguities / decisions needed (deduplicated)

- **RESOLVED: Rendering constants:** `RENDER_LAYOUT` in render.ts — judgementLineY 600, pixelsPerBeat 130, scratch 100px, keys 62px, cull [-40,740]. `pixelsPerBeat`, exact `judgmentLineY`, lane/note/scratch widths, culling margin — undefined; pick concrete values to make the y-formula real (playfield-rendering.md).
- **beat→ms order of operations** when a STOP and BPM-change coincide at the same beat, or STOP spans a BPM region — algorithm must be pinned (chart-format.md).
- **Global offset sign convention** (added to note time vs input time; +ms = notes feel later/earlier) — must be fixed once and used everywhere (audio-playback.md, judgement-scoring.md).
- **RESOLVED: Empty POOR trigger:** any mapped keydown with no note in that lane's ±250ms window triggers empty POOR; no cooldown. does any keypress with no note in-window penalize, or is there a proximity/cooldown gate — affects gauge fairness (judgement-scoring.md).
- **RESOLVED: Boundary inclusivity** of judgement windows: inclusive with a 1e-6 ms float epsilon (e.g. ±34ms → GOOD since 34 > 33.33). (open vs closed at 16.67/33.33/…); resolve the "±34ms GOOD boundary" wording in the acceptance criteria (judgement-scoring.md).
- **RESOLVED: Green-number reference BPM:** MAX BPM on song select (catalog has only song-level min/max pre-load; conservative for soflan); the in-play readout follows the live current BPM instead (scroll is BPM-proportional). Pinned in play-options.md SHOULD 13 (2026-07-17). was: choose most-frequent or max and document (play-options.md).
- **RESOLVED: RANDOM re-roll scope:** per play attempt, retries included (shell.ts passes no seed; startPlaySession rolls fresh per attempt). was: per play attempt (incl. retries) vs fixed per song-selection session (play-options.md).
- **Per-song offset (곡별오프셋)** storage/edit surface — no UI or location defined (audio-playback.md, chart-format.md).
- **RESOLVED: Practice BPM change timing:** next-loop-boundary (the acceptance criterion's reading); the change is "instant" at the UI level — the pending value shows immediately as NEXT LOOP BPM on the HUD and locks LOOP_PREP_AHEAD_SEC before the boundary. was: spec body vs acceptance criteria disagreement (practice-mode.md).
- **RESOLVED: Fade-out duration/curve** on abandon/fail: 300ms linear fade-out-then-stop (`STOP_FADE_MS`). — unspecified (audio-playback.md).
- **RESOLVED: CN note counting:** CN counts as 1 note everywhere (noteCount() = notes.length; maxEX = notes×2; gauge R denominator unchanged); the tail is penalty-only via the cnBreak extra event. was: 1 vs 2 — affects gauge R and EX math (chart-format.md, judgement-scoring.md).
- **RESOLVED: Records versioning / import collision:** export file = records.v1 envelope (version 1); import rejects any other version with a clear error (createLocalDoc migrate-hook seam still available for future v2); collision = per-chart field-wise best-merge, idempotent, never downgrades (specs updated 2026-07-17 — results-records.md SHOULD 10).
- **localStorage key names / JSON schemas** for settings, key map, play options, records — standardize centrally in `src/lib/storage` (multiple specs; settings-screen.md proposes conventions).
- **RESOLVED: Abandoned-play record policy:** Escape-abandon routes to RESULTS marked FAILED/abandoned (app-shell-navigation.md); abandoned/died plays (finishedSong=false) DO write playCount/lastPlayedAt and can set a FAILED lamp, but NEVER update bestExScore/bestRank/minBP — partial-run EX/BP is not comparable to a full play (documented in store.ts). whether an abandoned play writes a record (playCount, FAILED lamp) is unresolved (results-records.md).
- **RESOLVED: Calibration surface:** in-settings modal, not a separate screen-machine state (settings-screen.md open question; the screen enum in app-shell-navigation.md MUST 1 stays as-is).

## Key risks (with mitigations)

- **Cross-browser timing precision:** performance.now↔AudioContext.currentTime drift causes judgement error → `getOutputTimestamp()` with calibrated sampling fallback; assert ±2ms median δ stability over 10 replays (audio-playback.md).
- **60fps budget on 2,000-note charts:** per-frame allocation/GC stalls → sprite pooling, culling, no per-frame allocs; measure p95 frame-time ≤16.7ms in 10-min autoplay (playfield-rendering.md).
- **Backgrounded-tab rAF pauses** decoupling notes from audio → derive currentBeat solely from the audio clock every frame; test tab-return alignment (playfield-rendering.md).
- **IndexedDB quota/eviction** losing practice patterns → atomic writes, quota-error surfacing (practice-mode.md, `src/lib/storage/idb.ts`).
- **Autoplay-policy audio lock** before first gesture → create/resume AudioContext only after first key/click (title screen); handle suspended state (audio-playback.md, app-shell-navigation.md).
- **Logic/render entanglement blocking headless tests** → judgement/gauge/scoring pure and event-driven in `src/lib` + features; renderer strictly read-only (judgement-scoring.md, playfield-rendering.md).

## v1 Non-goals (do NOT add scope — from 00-overview.md)

- User song import (BMS) — built-in songs only (descoped 2026-07-16)
- Multiplayer, server-side records, online leaderboards
- Real-time keysound playback (unified to a single BGM track)
- BGA (background video) playback
- Gamepad / dedicated controllers, touchscreen input
- Double Play (DP), 5-key mode
- In-game pause during play (ESC = abandon song)
